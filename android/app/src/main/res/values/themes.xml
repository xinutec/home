<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- A framework theme, so no AppCompat. Black, so launch has no white flash. -->
    <style name="Theme.HomeWeb" parent="@android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@android:color/black</item>
    </style>
</resources>
